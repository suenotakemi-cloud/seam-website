# 正本はどこか（2026-08-15 決定）

障害のとき、どのファイルから作り直せばいいのかを迷わないための紙です。

## 予約Worker（seam-square-pay）

**正本＝この作業リポジトリの `booking/square/worker.js`**

- 出し方
  ```
  cd booking/square && npx wrangler deploy
  ```
- 設定は同じ場所の `wrangler.jsonc`。鍵（Squareのtoken等）はファイルには入っていません。
  `npx wrangler secret list` で名前だけ確認できます。
- 出す前に必ず通す関所
  ```
  npx wrangler deploy --dry-run
  ```
  `node --check` は通っても esbuild が落ちることがあります。**こちらが本当の関所です。**

### なぜ本番リポジトリ（seam-public）側ではないのか

`seam-public/.gitignore` の16行目が `booking/square/*.js` を除外しています。
つまり **seam-public を新しく clone しても worker.js は入っていません。**
seam-public のフォルダに置いてある worker.js は、gitに入っていない古い置き土産で、
存在しない8つのファイル（util.js / salon-bridge.js / salon-points.js / ai-chat.js /
precare.js / arrive.js / visit.js / membership.js）を読みに行くので**動きません**。
外からは見えません（404を確認済み）。消してよいものですが、
消す判断はオーナーに残してあります。

### 直近に出したもの

| いつ | 版 | 中身 |
|---|---|---|
| 2026-08-15 | e3894a12 | ① CORSに X-Staff-Token / X-Kiosk-Token を追加 |
| 2026-08-15 | 56d3853a | ② カウンセリング端末の読み取り制限・確認コードを暗号乱数へ |
| 2026-08-15 | 6731a9f9 | ③ 前金の金額をサーバーで計算し直す |
| 2026-08-15 | 2076b0b6 | ③の移行措置（古い予約ページからの前金を通す） |

戻すとき
```
npx wrangler rollback --version-id <上の版ID>
```

## ホームページと画面（seam.site）

**正本＝`seam-public/`（GitHubの suenotakemi-cloud/seam-website、main）**

- push すると Cloudflare Pages が自動で出します。
- 作業リポジトリ側の同じ名前のファイルは**下書き**です。
  直したら seam-public へコピーして push してください。

## 金額の決まり

**正本＝`saas/checkout.js`**

前金の計算（`depositFor`）は、画面とサーバーの両方がここを使います。
2か所に書き写すと、片方だけ直したときに
「正しい画面からの支払いが弾かれる」という壊れ方をします。

テスト
```
cd saas && node dep-test.mjs
```
