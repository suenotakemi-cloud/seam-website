name: Build finder (precompile JSX)

# finder の JSX ソースが変わったら、Babelで素のJSへ事前コンパイルして commit する。
# これにより本番 finder.html はブラウザ内 babel.min.js(2.7MB) を読まずに済む。
on:
  push:
    paths:
      - 'js/finder-app.jsx'
      - '.github/scripts/build-finder.js'
      - '.github/workflows/build-finder.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Babel インストール
        run: npm init -y && npm i --no-save @babel/core@^7 @babel/preset-react@^7

      - name: finder-app.jsx を事前コンパイル
        run: node .github/scripts/build-finder.js

      - name: 生成物をコミット
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add js/finder-app.js
          git diff --staged --quiet && { echo "no change"; exit 0; }
          # [skip ci]は付けない: CF Pagesが[skip ci]入りコミットの配信をスキップし
          # finder-app.jsが本番に出ない原因になる(従来は finder.html ?v= bump=人間pushでしか出なかった)。
          # js出力はこのworkflowのトリガー外＋GITHUB_TOKEN pushは新workflow非起動でループしない
          # (update-ranking同様[skip ci]無しbotコミットはCFが自動配信する)。
          git commit -m "build: precompile finder-app.js"
          git pull --rebase origin main
          git push
