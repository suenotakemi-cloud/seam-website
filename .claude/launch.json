{
  "version": "0.0.1",
  "configurations": [
    { "name": "seam", "runtimeExecutable": "python3", "runtimeArgs": ["-I","-S","-c","import os,http.server,socketserver,functools;h=functools.partial(http.server.SimpleHTTPRequestHandler,directory='/tmp/seam-preview');socketserver.TCPServer(('',int(os.environ.get('PORT','3000'))),h).serve_forever()"], "port": 3000 }
  ]
}
